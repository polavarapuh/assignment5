import React from "react";

const Header = () => {
  const headerStyle = {
    textAlign: "center",
    padding: "10px",
    backgroundColor: "green",
    color: "white",
    fontSize: "24px",
  };

  return <div style={headerStyle}>Weather App</div>;
};

export default Header;
